# Session Summary — 2026-09-15

Sound + treasure-hunt feature session, merged to `origin/main` and verified
on-device. Shipped in three commits on top of the ongoing `game-header-layout`
feature line: `de575e2` (feat), `449283f` (merge of `origin/main`), `741f94e`
(fix) — pushed to **`origin/main`** (Subhamcr7/puzzled) at **`741f94e`**. CI
(`android-apk.yml`, run `34881862482`) built and published the release APK as
**ci-41** (`puzzled-ci-41.apk`), a debug-signed `com.puzzled.app` build — the
first APK shipped from `main` this feature line.

---

## 1. Scope

- **Global UI sounds** behind the existing Sound toggle: `playUiTap()` on every
  Pressable (board-audio `board-audio.ts` registers app-wide tap + coin-gain;
  `src/shared/ui/ui-sound.ts` bridge), wallet emits coin-gain only when
  `deltaCoins > 0`, no looping of `coin-gain.mp3`.
- **Daily Treasure Hunt:** derivation-only 7-stop journey (no new storage).
  `treasure.ts` computes the trail from the completions log via
  `streakFrom`/`dateKey`/`pickDailyPuzzle`; tiers Easy (1–2) / Medium (3–5) /
  Hard (6–7); rewards `20 + (stop-1)*10` (stop 7 = 200); map title
  `Day ${streak} — stop ${stop} of 7 · ${tier}` else `Start the trail`; reached
  nodes open `/puzzles`, the current node opens `/daily`, locked nodes are inert
  (`testID="treasure-map"`, defensive `scrollRef.scrollTo?.`).
- **Tests:** 13 screen/unit files written or extended — treasure, home,
  library, daily, achievements, coins, themes, settings, statistics,
  board-audio, ui-sound + wallet/game-header/puzzle-tile/puzzles-screen/
  piece-picker coverage. Suite grew **44 → 55**, tests **441 → 493**.

## 2. Merge of `origin/main` (conflict resolution policy)

`main` (13 commits behind the feature line: fonts, native splash, docs) and the
feature branch both redesigned several shared files, so the merge at `449283f`
conflicted on 9 files:

| file | resolution |
|---|---|
| `src/shared/ui/index.ts` | **union** — kept local exports + `PressDarken`/`usePressProgress` |
| `home-screen.tsx`, `LoadingScreen.tsx`, `PopButton(.test)`, `PopTabBar.tsx`, `fonts.test.ts` | **ours** (local theme/Text/splash-handoff architecture) |
| `splash.test.ts` | **theirs** — merged `app.json` pins the intentionally-blank `assets/images/splash-blank.png` (`sha256 c05bcde1…`); the local test pinned the old bear hash |

### 2.1 Post-merge regression caught by tests

The first full run after merging failed exactly 2 treasure-screen tests:
"shows the day, stop count and difficulty tier" expected `Day 1 — stop 1 of 7 ·
Easy` but rendered `Start the trail` (streak 0), and the reward test found the
right title but `recordOnce` never fired.

**Root cause:** the test helper built day keys with `date.toISOString().slice
(0,10)` (UTC), while the app's `dateKey`/`streakFrom` (`src/data/daily.ts`) use
**local** midnight. The suite was green exactly while UTC and local agreed on
"today"; once local midnight crossed (and after the merge's timing), a
`dayKey(-1)` completion bucketed two local days back, so `streakFrom` walked
over an empty day and the payment effect never re-ran. Fix (`741f94e`): the
test `dayKey()` now uses the app's own `dateKey(new Date() + offset days)` —
local, deterministic on every device/timezone, and `12:00Z` completedAt stamps
bucket to the same local day in every `±12h` timezone. No product code changed.

## 3. Local gates (all green at `7091c0a`)

`typecheck` 0, `lint` 0, `prettier --check` clean, **`npm test`: 55 suites /
506 tests pass**. `npm run verify` (incl. `expo export` JS bundle) also green.

## 4. Build & release (no local build — CI only)

Standing rule established this session: APKs are always built in GitHub Actions,
never locally. A local `expo run:android` was attempted, failed at toolchain
setup and was abandoned; Android SDK was bootstrapped to `H:\Tools\android sdk`
(junction `H:\Tools\androidsdk`, android-36, build-tools 36.0.0, ndk
27.1.12297006), used only to see the device, then deleted (see §6).

- Pushed `game-header-layout` → `origin/main` (`8959932..741f94e`, fast-forward
  via the merge commit).
- CI run **`34881862482`** (push to `main`): Typecheck / Lint / Test (493) /
  `expo prebuild` / `assembleRelease` all green.
- Prerelease **ci-41** → **`puzzled-ci-41.apk`** (177,845,093 B, debug-signed —
  no keystore configured yet).

### 4.1 Subsequent CI (corner-snap fix session)

- **`f795a9d`** — workflow fix: `setup-android` `packages: platform-tools` (Google
  removed legacy `tools` package; `sdkmanager` failed without this).
- **`143e955`** — eviction fix + silent grab + tests. Pushed to `origin/main`.
  CI run `34933623849` (push): code gates pass, setup-android failed (pre-fix).
  Rerun failed same. CI run `34934214047` (after `f795a9d`): setup-android +
  prebuild + gradle green; **canceled by user** mid-Assemble APK (was building
  Release; user wanted Debug only).
- **`7091c0a`** — all-four-corner tests + audio structural guards. Committed on
  `game-header-layout`, pushed to `origin/game-header-layout` (NOT main — to
  avoid auto-Release build).
- Debug build dispatched via `gh workflow run -f variant=debug` on
  `game-header-layout`. CI run **`34936423435`**: all gates green (typecheck,
  lint, test 506, expo prebuild, assembleDebug). Prerelease **ci-44** →
  **`puzzled-ci-44.apk`** (345 MB debug, no embedded JS bundle).

## 5. On-device verification (OnePlus CPH2487, serial `d6edd1ce`)

CI-build APK installed over adb (`adb uninstall com.puzzled.app` first because
the debug key changes every build; fresh install ⇒ storage reset, 500-coin
starter grant) and launched via `monkey`.

- **Home live**, then walked: Home → Daily Treasure Hunt (`Open`) →
  "Start the trail" + stop-1 reward text correct for a fresh store; Daily →
  `Play Now` → Select Difficulty; tap sound confirmed by the Expo audio
  `MediaSessionService` `PLAYING` transitions firing per press.
- **Piece-picker reward pill works live**: default 4×4 = `Reward: 26`
  (`10+4²`); tapping Easy (9, 3×3) updated the pill to **19**.
- Screenshots captured for human eyeball: `shot-treasure.png`, `shot-game1.png`
  (`C:\Users\User\AppData\Local\Temp\opencode\pzudl`, since deleted).

### 5.1 adb input-tap quirk (documented, not a product bug)

Injected `adb shell input tap` reached mid-screen controls (chips, whole
treasure card) but **not** the top-left back arrow nor the bottom Start Puzzle
button on Select Difficulty — those taps only produced the ambient audio loop,
with the screen verified pixel-identical across 4 timed frames after the tap
(touch never reached RN; window is full-screen edge-to-edge, 1240×2772, no
inset). Real finger taps worked on every control. Likely a per-device
touch-injection/gesture-band sensitivity quirk of this OPPO build's injected
touches near the display edges, **not** app code — recorded so future adb
driving picks targets mid-screen.

### 5.2 Corner snap bug — fix attempted, still reproduces on-device

**Original report (§5.2 in prior session):** after mis-dropping a piece at/near
a corner, the correct corner piece **no longer snaps** when placed aligned — no
"clunk", stays loose. Investigation found: snap is center-distance only vs the
piece's OWN solved slot; a wrong piece never "locks" at a foreign corner; loose
pieces render **after** locked clusters (`puzzle-board.tsx` ~1787/1798), so a
loose miss always draws over anything that then locks beneath it, and hit-testing
(`looseBoxes` topmost-first, ~1443) makes each new grab at that corner pick up
the covering loose piece instead — reads as "the correct piece won't fit again."
`clampPieceToBoard` (`tray-geometry.ts:234`) pins overhanging drops at the
corner (position → (0,0)), which parks misses exactly on the corner slot.

**Fix attempted (`143e955`):** after a successful snap, `dropPiece` now evicts
loose pieces that overlap the freshly locked slot back to the tray
(`position.y = boardHeight`, same mechanism as a tray-drop), clearing the
covering piece so the correct piece shows and stays grabbable. Extended
`DropPieceInput` with optional `boundsById` + `boardHeight`; `boxesIntersect`
checks AABB overlap; `buildNextSession` refactored out of `withUpdatedPiece`.

**On-device result: bug still reproduces.** The unit tests pass (9 eviction
tests covering all 4 corners + centre + guards, 506 total), but the physical
Pan gesture on the OnePlus CPH2487 still shows the correct piece not snapping
after a mis-drop covers its slot. **Root cause not yet identified** — possible
candidates:

- `releaseGeometry` may not include entries for tray pieces, so `boundsById`
  could be incomplete (missing pieces not currently rendered on the board).
- The `piece.position.y < boardHeight` guard may filter out pieces that are
  visually on the board but whose computed position exceeds `boardSize.height`
  due to coordinate-space differences.
- The AABB intersection may be too strict (touching edges don't count) or the
  `boardHeight` parameter may not match the actual board boundary used by the
  tray layout.

**Also reported:** a similar mis-drop covering a **middle (non-corner) slot**
exhibits the same failure — the correct piece doesn't snap when placed over a
covering loose piece in the centre of the board.

**Not yet fixed.** A debug APK (`ci-44`) was built, installed, and confirmed
booting to the expo-dev-client launcher, but the eviction fix needs further
investigation before the on-device corner/centre acceptance test can pass.

## 6. Cleanup (this session)

- Deleted the project-local `sound/` folder (byte-identical duplicates of the
  committed `assets/audio/*.mp3`; never committed).
- Removed all local build residuals: `android/.gradle`, `android/build`,
  `android/app/build`, `android/local.properties` (`android/` stays gitignored,
  regenerated by CI via `expo prebuild`).
- Removed `~/.gradle` (~4.5 GB) and the temp SDK installer zip from C:.
- Deleted the Android SDK + tools at `H:\Tools\android sdk` /
  `H:\Tools\androidsdk` and the stale `ANDROID_HOME` / `ANDROID_SDK_ROOT` user
  environment variables (CI is the only build path).
- Removed the temp adb platform-tools / APK / screenshots dir.

## 7. Known items / caveats

- APK is **debug-signed** (17 MB/short of a "small" release build; 170 MB for
  release, 345 MB for debug). Uninstall the old install before installing a new
  build (new debug key each time). A production keystore + `.github/workflows`
  signing step is still TODO.
- Actions annotations (pre-existing, non-blocking): `setup-java@v4` deprecated →
  v5, Node-20 actions now auto-run on Node 24.
- Remotes in play: `origin` = Subhamcr7/puzzled (this session's push target),
  `newapp` = Sayandeep1013/puzzled. The `android-apk.yml` workflow auto-fires on
  push to `main` and `design/**`; pushes to `game-header-layout` do not
  auto-build (dispatch `workflow_dispatch` debug manually if needed).
- adb on this host: MSYS mangles leading `/` in device paths — pass them
  without the slash (`adb pull "sdcard/x"`) or set `MSYS_NO_PATHCONV=1`.
- The corner-snap + middle-piece eviction bug (§5.2) is **still reproducing
  on-device** despite the unit-tested fix in `143e955`. The `releaseGeometry` /
  `boundsById` pipeline needs further investigation — likely a coordinate-space
  mismatch or incomplete piece mapping. Debug APK `ci-44` is installed on the
  device (`d6edd1ce`) with Metro running for manual testing.
- Branch state: `game-header-layout` at `7091c0a` (test additions); `origin/main`
  at `f795a9d` (fix + workflow patch). Tests are on the work branch only — not
  merged to main to avoid triggering a Release build.